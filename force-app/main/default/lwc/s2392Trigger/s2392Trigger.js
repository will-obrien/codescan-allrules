// This component is generated to trigger the SonarJS rule: javascript:S2392
import { LightningElement } from 'lwc';

export default class s2392Trigger extends LightningElement {
    // Rule: Variables should be used in the blocks where they are declared
    connectedCallback() {
        // This component triggers: javascript:S2392
        console.log('Triggering SonarJS rule: javascript:S2392');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S2392";
        return example;
    }
}