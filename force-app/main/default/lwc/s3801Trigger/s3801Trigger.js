// This component is generated to trigger the SonarJS rule: javascript:S3801
import { LightningElement } from 'lwc';

export default class s3801Trigger extends LightningElement {
    // Rule: Functions should use "return" consistently
    connectedCallback() {
        // This component triggers: javascript:S3801
        console.log('Triggering SonarJS rule: javascript:S3801');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3801";
        return example;
    }
}