// This component is generated to trigger the SonarJS rule: javascript:S4524
import { LightningElement } from 'lwc';

export default class s4524Trigger extends LightningElement {
    // Rule: "default" clauses should be last
    connectedCallback() {
        // This component triggers: javascript:S4524
        console.log('Triggering SonarJS rule: javascript:S4524');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S4524";
        return example;
    }
}