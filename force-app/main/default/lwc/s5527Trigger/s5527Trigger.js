// This component is generated to trigger the SonarJS rule: javascript:S5527
import { LightningElement } from 'lwc';

export default class s5527Trigger extends LightningElement {
    // Rule: Server hostnames should be verified during SSL/TLS connections
    connectedCallback() {
        // This component triggers: javascript:S5527
        console.log('Triggering SonarJS rule: javascript:S5527');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S5527";
        return example;
    }
}