// This component is generated to trigger the SonarJS rule: javascript:S6092
import { LightningElement } from 'lwc';

export default class s6092Trigger extends LightningElement {
    // Rule: Chai assertions should have only one reason to succeed
    connectedCallback() {
        // This component triggers: javascript:S6092
        console.log('Triggering SonarJS rule: javascript:S6092');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S6092";
        return example;
    }
}