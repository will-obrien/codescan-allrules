// This component is generated to trigger the SonarJS rule: javascript:S3972
import { LightningElement } from 'lwc';

export default class s3972Trigger extends LightningElement {
    // Rule: Conditionals should start on new lines
    connectedCallback() {
        // This component triggers: javascript:S3972
        console.log('Triggering SonarJS rule: javascript:S3972');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3972";
        return example;
    }
}