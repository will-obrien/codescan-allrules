// This component is generated to trigger the SonarJS rule: javascript:S117
import { LightningElement } from 'lwc';

export default class s117Trigger extends LightningElement {
    // Rule: Variable, property and parameter names should comply with a naming convention
    connectedCallback() {
        // This component triggers: javascript:S117
        console.log('Triggering SonarJS rule: javascript:S117');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S117";
        return example;
    }
}