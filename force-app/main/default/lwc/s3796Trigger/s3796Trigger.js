// This component is generated to trigger the SonarJS rule: javascript:S3796
import { LightningElement } from 'lwc';

export default class s3796Trigger extends LightningElement {
    // Rule: Callbacks of array methods should have return statements
    connectedCallback() {
        // This component triggers: javascript:S3796
        console.log('Triggering SonarJS rule: javascript:S3796');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3796";
        return example;
    }
}