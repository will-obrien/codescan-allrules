// This component is generated to trigger the SonarJS rule: javascript:S3834
import { LightningElement } from 'lwc';

export default class s3834Trigger extends LightningElement {
    // Rule: "Symbol" should not be used as a constructor
    connectedCallback() {
        // This component triggers: javascript:S3834
        console.log('Triggering SonarJS rule: javascript:S3834');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3834";
        return example;
    }
}