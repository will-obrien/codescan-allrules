// This component is generated to trigger the rule: cs-js:no-param-reassign
import { LightningElement } from 'lwc';

export default class noparamreassignTrigger extends LightningElement {
    // Rule: disallow reassigning `function` parameters
    connectedCallback() {
        // This component triggers: cs-js:no-param-reassign
        console.log('Triggering rule: cs-js:no-param-reassign');
    }
}