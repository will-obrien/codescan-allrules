// This component is generated to trigger the SonarJS rule: javascript:S3516
import { LightningElement } from 'lwc';

export default class s3516Trigger extends LightningElement {
    // Rule: Function returns should not be invariant
    connectedCallback() {
        // This component triggers: javascript:S3516
        console.log('Triggering SonarJS rule: javascript:S3516');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3516";
        return example;
    }
}