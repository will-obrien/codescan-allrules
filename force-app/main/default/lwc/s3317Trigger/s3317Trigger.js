// This component is generated to trigger the SonarJS rule: javascript:S3317
import { LightningElement } from 'lwc';

export default class s3317Trigger extends LightningElement {
    // Rule: Default export names and file names should match
    connectedCallback() {
        // This component triggers: javascript:S3317
        console.log('Triggering SonarJS rule: javascript:S3317');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3317";
        return example;
    }
}