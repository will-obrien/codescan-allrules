// This component is generated to trigger the SonarJS rule: javascript:S3863
import { LightningElement } from 'lwc';

export default class s3863Trigger extends LightningElement {
    // Rule: Imports from the same modules should be merged
    connectedCallback() {
        // This component triggers: javascript:S3863
        console.log('Triggering SonarJS rule: javascript:S3863');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3863";
        return example;
    }
}