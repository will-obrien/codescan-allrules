// This component is generated to trigger the SonarJS rule: javascript:S6353
import { LightningElement } from 'lwc';

export default class s6353Trigger extends LightningElement {
    // Rule: Regular expression quantifiers and character classes should be used concisely
    connectedCallback() {
        // This component triggers: javascript:S6353
        console.log('Triggering SonarJS rule: javascript:S6353');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S6353";
        return example;
    }
}