// This component is generated to trigger the SonarJS rule: javascript:S3512
import { LightningElement } from 'lwc';

export default class s3512Trigger extends LightningElement {
    // Rule: Template strings should be used instead of concatenation
    connectedCallback() {
        // This component triggers: javascript:S3512
        console.log('Triggering SonarJS rule: javascript:S3512');
        
        // Example code that might trigger the rule
        var example = "This triggers rule javascript:S3512";
        return example;
    }
}